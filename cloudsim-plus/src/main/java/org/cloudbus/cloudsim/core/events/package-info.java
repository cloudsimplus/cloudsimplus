/**
 * Provides classes used by the core message passing mechanisms.
 * CloudSim Plus is an event discrete simulator that relies on message
 * transmission to update the progress of the simulation.
 * Such a package provide classes that represent core simulation
 * events and event queues.
 *
 * @author Manoel Campos da Silva Filho
 */
package org.cloudbus.cloudsim.core.events;
